NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_EXPLORER=https://testnet.arcscan.app

# Shows manual move controls on the rental page. The fleet drives itself, so these only exist
# to stand in for a simulator that is not attached yet.
NEXT_PUBLIC_SIMULATOR_CONTROLS=false
