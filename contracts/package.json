{
  "name": "canopy-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "hardhat build",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network arcTestnet",
    "seed": "hardhat run scripts/seed-fleet.ts --network arcTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "hardhat": "^3.15.0",
    "typescript": "^5.7.0",
    "viem": "^2.21.0"
  },
  "dependencies": {
    "forge-std": "github:foundry-rs/forge-std"
  }
}
